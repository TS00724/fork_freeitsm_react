# Feature convention

No business feature is implemented before G1. After approval, one feature owns its routes, pages, components, API adapters, types, and tests. Shared code moves out only after at least two real features demonstrate the same stable need.
