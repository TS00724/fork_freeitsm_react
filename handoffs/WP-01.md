# WP-01 handoff — baseline, inventory, and migration controls

## Git baseline

- Repository: `TS00724/fork_freeitsm_react`
- Exact target URL: `https://github.com/TS00724/fork_freeitsm_react.git`
- Start SHA: `bfad6b0db7242686114143cc590a146871a44b21`
- End SHA: `b703183176db6c4ff56b6860725220cf8914d1fa`
- Branch: `main`
- Visible branches: `main` only
- Worktree status: target local worktree unavailable; remote branch was immutable at the recorded start SHA
- Pull request: none created

## Files read

`progress/MIGRATION_MASTER_PLAN.md`, `progress/WORK_PROGRESS.md`, root README and
build/deployment files, `.htaccess`, current entry/config, public v1 controller
and routes, request/session security, shared services directory, and the remote
recursive tree. Root `AGENTS.md` and prior handoff/decision/API/route/verification
files were not present.

## Repository scale

Latest full audit: approximately 2,021 files, 1,657 PHP, 167 browser JS, 45 CSS,
about 277 PHP UI surfaces, about 719 legacy browser PHP endpoints, about 203
public v1 routes, and 22 product modules plus separate user surfaces. These are
source-audit counts, not a fabricated fresh local recount.

## Existing frontend result

No `frontend/`, React/TSX, Vite, TSConfig, frontend package manifest, or lockfile
existed at the start SHA. No earlier scaffold was overwritten.

## PHP UI/API boundary

- legacy PHP pages remain the active UI;
- `/api/v1` remains the external API-key API;
- legacy session endpoints remain untouched;
- callbacks, auth pretty URLs, CSAT, QR, cron, file, feed, webhook, and stream routes remain authoritative;
- `includes/services/` is a future reuse seam;
- complete UI CSRF/session/tenant/RBAC contracts are not part of WP-01/WP-02.

## Risks and blockers

1. No mounted checkout for literal local status/uncommitted-change inspection.
2. npm registry DNS unavailable for WP-02 dependency resolution.
3. EUI license/notice decision remains open.
4. Runtime-config host and BrowserRouter fallback require G1 approval.
5. BFF/session/security contracts remain intentionally unknown.

## Accurate WP-02 entry

Begin with `frontend/package.json`, `frontend/index.html`, `frontend/src/main.tsx`,
providers, router, runtime config, AppShell, errors, tests, and code-reading guide.
Do not touch PHP or start any business feature/BFF route.

## Next files to read

1. `progress/REPOSITORY_INVENTORY.md`
2. `progress/API_MATRIX.md`
3. `progress/ROUTE_MAP.md`
4. `progress/DECISION_LOG.md`
5. `progress/VERIFICATION_REPORT.md`
6. `frontend/package.json`
7. `frontend/src/main.tsx`
8. `frontend/src/app/providers/AppProviders.tsx`
9. `frontend/src/app/router.tsx`
10. `frontend/src/config/runtimeConfig.ts`

## Actions confirmation

No GitHub Actions workflow was created, modified, executed, or depended upon.

## Verification-closure addendum — 2026-08-21

The historical missing-worktree blocker is closed. Literal `git status`, branch,
HEAD, and remote commands were run in the target checkout. The source closure
commit is `46c901597557abe7f319a880c9a3539105307196`; the worktree was clean after
that commit, and the only configured remote is the user's fork `origin`.

WP-01 is now `Verified complete`: Implementation, API/Contract, Verification,
and Docs/Handoff are all 100%, Confidence Yes. No upstream remote, pull request,
force push, or GitHub Actions workflow was used.
