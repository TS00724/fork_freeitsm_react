# WP-02 handoff — isolated React 18 + TypeScript + EUI foundation

## Scope and Git

- Repository: `TS00724/fork_freeitsm_react`
- Branch target: `main`
- Start SHA: `b703183176db6c4ff56b6860725220cf8914d1fa`
- End SHA: `75083fbef3ac1bece1b2a7397b4d60abf9456ad5`
- Pull request: none
- Force push: none
- GitHub Actions: not created, changed, run, or used

## Original versus new work

At the baseline there was no `frontend/`, package manifest, lockfile, React/TSX,
Vite, or frontend test. WP-02 adds the isolated foundation; no earlier scaffold
was overwritten.

## Implemented

- React 18 / strict TypeScript / EUI Borealis / Vite / React Router manifest;
- runtime `BASE_URL`, app path, locale, timezone, and color-mode configuration;
- provider order for runtime config, theme, extension points, unresolved auth,
  unresolved tenant, unresolved permission, and BrowserRouter;
- minimal AppShell and architecture-review-only navigation;
- Error Boundary plus 403, 404, and generic error skeletons;
- light/dark toggle;
- API transport placeholder with same-origin credentials and optional CSRF hook,
  but no endpoint or response contract;
- empty feature convention and unit/component test skeletons;
- isolated `frontend/dist/` output and local verification scripts;
- code-reading guide, architecture diagram, and third-party review.

## Explicitly not implemented

No PHP UI BFF, `/api/ui/v1` handler, session/bootstrap, CSRF validation, RBAC,
tenant enforcement, Watchtower, Tickets, Assets, CMDB, or any other business
module. No PHP file, root routing rule, Docker definition, or legacy UI file was
modified.

## Actual validation

| Command/check | Exit | Result |
|---|---:|---|
| `npm run verify:structure` | 0 | Passed, 14 required files |
| `npm run verify:isolation` | 0 | Passed |
| `npm run verify:lockfile` | 1 | Expected blocker: lockfile missing |
| `npm ci --ignore-scripts --fetch-retries=0 --fetch-timeout=3000` | 1 | npm requires lockfile |
| `npm run typecheck` | 1 | Blocked: dependency type definitions absent |
| `npm run lint` | 127 | Blocked: ESLint not installed |
| `npm run test` | 127 | Blocked: Vitest not installed |
| `npm run build` | 1 | Blocked before Vite build by missing types |
| `npm run dev -- --host 127.0.0.1` | 127 | Blocked: Vite not installed |
| disposable `npm install --package-lock-only ...` | 124 | Timed out after npm registry `EAI_AGAIN` failures |
| dependency-free runtime/API assertions | 0 | Passed |
| early runtime `<base>` subdirectory assertion | 0 | Passed |
| static scope/isolation assertions | 0 | Passed |

No failed or unexecuted dependency-backed test is described as passing.

## Progress judgement

Implementation 95%, API/Contract 100% for the placeholder-only WP-02 scope,
Verification 35%, Docs/Handoff 100%. Effective progress is **35%**. Confidence is
**No** and status is **Blocked** because there is no trustworthy lockfile and the
full install/typecheck/lint/test/build/start evidence is unavailable.

## Unblock and review

1. Restore npm registry access.
2. In `frontend/`, run `npm install --package-lock-only --ignore-scripts`.
3. Review the resolved graph, peer compatibility, LICENSE and NOTICE files.
4. Commit the real lockfile.
5. Run `npm ci` and `./scripts/verify-local.sh`.
6. Start the app and manually inspect root, direct 403/404 deep links, theme,
   subdirectory basename, and build output.
7. Update verification/progress; only then may WP-02 become Verified complete.
8. Read `docs/react-migration/CODE_READING_GUIDE.md` and decide the G1 checklist.

## Next files for the user

1. `frontend/package.json`
2. `frontend/DEPENDENCY_INSTALL_BLOCKER.md`
3. `frontend/src/main.tsx`
4. `frontend/src/app/providers/AppProviders.tsx`
5. `frontend/src/app/router.tsx`
6. `frontend/src/config/runtimeConfig.ts`
7. `frontend/src/api/client.ts`
8. `frontend/src/layouts/AppShell.tsx`
9. `frontend/src/theme/ThemeProvider.tsx`
10. `docs/react-migration/CODE_READING_GUIDE.md`

**已停止在 G1，等待用户审核，尚未开始 BFF。**
